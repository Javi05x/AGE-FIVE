# Frontend (React + Vite) — AGE-FIVE

## Setup
```bash
cd frontend
npm install
cp .env.example .env
```

## Ejecutar
```bash
npm run dev
```

Asegúrate de que el backend esté en `http://localhost:8000` (o ajusta `VITE_API_URL`).
